import { ComponentPropsWithRef, ReactNode } from 'react';

interface TabBarProps extends ComponentPropsWithRef<any> {
  children: ReactNode
}
interface TabBarItemProps extends ComponentPropsWithRef<any> {
  children: ReactNode
}